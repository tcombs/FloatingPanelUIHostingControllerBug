import SwiftUI

struct HostingControllerExampleView: View {
    var body: some View {
        VStack {
            Text("I am a SwiftUI view inside of a UIHostingController.")
                .padding(.top)
            Spacer()
        }
        .padding()
    }
}
